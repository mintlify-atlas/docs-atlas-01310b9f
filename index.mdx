---
title: "Ollama Provider for PHP AI SDK"
sidebarTitle: "Home"
description: "Official Ollama provider for the PHP AI SDK. Run local LLMs for text generation, streaming, embeddings, and image generation — no API key required."
---

The PHP AI SDK Ollama provider connects your PHP application to any model running on a local or remote Ollama server. It supports the full generation surface — text, streaming, embeddings, and experimental image generation — through a clean, framework-agnostic interface that works with any PSR-18 HTTP client.

<CardGroup cols={2}>
  <Card title="Installation" icon="box" href="/installation">
    Install via Composer and get the provider running in under a minute
  </Card>
  <Card title="Quickstart" icon="bolt" href="/quickstart">
    Generate your first response from a local Ollama model
  </Card>
  <Card title="Guides" icon="book-open" href="/guides/text-generation">
    Explore text generation, streaming, embeddings, and image generation
  </Card>
  <Card title="API Reference" icon="code" href="/api-reference/ollama-facade">
    Full reference for the Ollama facade, provider, models, and types
  </Card>
</CardGroup>

## What's included

<CardGroup cols={2}>
  <Card title="Text Generation" icon="message" href="/guides/text-generation">
    Chat Completions and Responses API support, with vision, tool calling, reasoning, and structured output
  </Card>
  <Card title="Streaming" icon="wave-sine" href="/guides/streaming">
    Real-time server-sent event streaming for any installed text model
  </Card>
  <Card title="Embeddings" icon="vector-square" href="/guides/embeddings">
    Native batch embeddings via Ollama's `/api/embed` endpoint with dimension control
  </Card>
  <Card title="Image Generation" icon="image" href="/guides/image-generation">
    Experimental image generation through Ollama's OpenAI-compatible endpoint
  </Card>
  <Card title="Model Discovery" icon="magnifying-glass" href="/guides/model-discovery">
    List installed models and inspect their capabilities from the live server
  </Card>
  <Card title="Configuration" icon="gear" href="/configuration/options">
    Base URL, native URL, API key, and per-request API endpoint overrides
  </Card>
</CardGroup>

## Getting started

<Steps>
  <Step title="Install the package">
    Add the provider to your project with Composer:
    ```bash
    composer require aisdk/ollama
    ```
  </Step>
  <Step title="Start Ollama">
    Make sure Ollama is running locally with at least one model pulled:
    ```bash
    ollama pull llama3.2
    ollama serve
    ```
  </Step>
  <Step title="Generate your first response">
    ```php
    use AiSdk\Generate;
    use AiSdk\Ollama;

    $result = Generate::text()
        ->model(Ollama::model('llama3.2'))
        ->prompt('Explain closures in PHP.')
        ->run();

    echo $result->text;
    ```
  </Step>
</Steps>

<Note>
  No API key is required for a local Ollama server. The provider defaults to `http://localhost:11434/v1`. See [Configuration](/configuration/options) if your server runs elsewhere or requires authentication.
</Note>
